{% extends 'base.html.twig' %}

{% block body %}

<article> 

<h2> {{article.title}} </h2> 

<div class="metadata" > {{article.createdAt | date('d/m/Y')}} a  {{article.createdAt | date('H:m')}}  dans la categorie politique </div> 
<div class="content"> 

<img src=" {{article.image}}" alt=" " > 

<hr>
 {{article.content | raw}}
 </hr> 
</div> 
</article>


{% endblock %}
